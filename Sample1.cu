int z;
int gug(int z, char *v); // function declaration
int main(int c, int b)
{
    char *b; /*yoloo */
    int x = z[3] + 5; //huehueheu
    b = a[9];
    int a[10];
    fun(x); // function call
    if (x == 0)
    {
        char *c;
    }
    else
    {
        x = 0;
    }
    if (p == 'b')
        c = p + f;

    while (y != 0)
    {
        int p,q;
    }

    while (true)
    y=-1;

    while (0)
    C='N';
    
    return 1000;
}